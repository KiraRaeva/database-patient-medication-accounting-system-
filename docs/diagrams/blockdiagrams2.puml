@startuml
!theme plain

title Блок-схема процесса работы пациента

start
:Запуск приложения;
:Вход в личный кабинет;

if (Доступ разрешен?) then (Да)
  :Открытие персональной панели;
else (Нет)
  :Ошибка доступа;
  stop
endif

while (Работать в системе?) is (Да)
  :Выбор доступного действия;

  if (Действие?) then (Мои назначения)
    :Просмотр текущих препаратов;
    :Детали дозировки и времени;
  elseif (Действие?) then (Прием лекарств)
    :Подтверждение приема препарата;
    :Отметка времени приема;

    if (Отметить самочувствие?) then (Да)
      :Запись состояния после приема;
      :Сохранение в истории;
    else (Нет)
      :Пропуск отметки;
    endif
  else (Выход)
    :Завершение сеанса;
    stop
  endif

  :Обновление статуса в системе;
endwhile

:Завершение работы;
stop

@enduml